{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://structura.local/contracts/model_outputs/granite_invoice_line_items.v1.schema.json",
  "title": "Granite Invoice Line Items v1",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "line_items": {
      "type": "array",
      "maxItems": 40,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["description"],
        "properties": {
          "ordinal": {"type": "integer", "minimum": 1},
          "description": {"type": "string", "minLength": 1},
          "quantity": {"type": ["number", "string", "null"]},
          "unit": {"type": ["string", "null"]},
          "unit_price": {"type": ["number", "string", "null"]},
          "amount": {"type": ["number", "string", "null"]},
          "category_hint": {"type": ["string", "null"]},
          "row_index": {"type": ["integer", "string", "null"]},
          "table_id": {"type": ["string", "null"]},
          "page_number": {"type": ["integer", "string", "null"]}
        }
      }
    },
    "totals": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "subtotal": {"type": ["number", "string", "null"]},
        "tax_total": {"type": ["number", "string", "null"]},
        "shipping_total": {"type": ["number", "string", "null"]},
        "discount_total": {"type": ["number", "string", "null"]},
        "total": {"type": ["number", "string", "null"]}
      }
    },
    "confidence": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "overall": {"type": ["number", "null"], "minimum": 0, "maximum": 1},
        "schema_fit": {"type": ["number", "null"], "minimum": 0, "maximum": 1},
        "table_structure": {"type": ["number", "null"], "minimum": 0, "maximum": 1}
      }
    }
  },
  "required": ["line_items"]
}
