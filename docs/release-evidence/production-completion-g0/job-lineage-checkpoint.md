# JOB-02 ancestry checkpoint

Migration092 adds monotonic execution_generation, captured parent_execution_generation, and permanent lineage_revoked_at. It reuses parent_job_id, makes parent edges and HH/document/batch scope immutable, prevents generation decreases/revocation clearing, and changes the parent FK to deferred NO ACTION so deleting a parent cannot orphan an authoritative child. Roots already present remain roots; legacy ancestry is not inferred.092 is registered; root owns other registry lines.

Child creation captures the explicitly scoped worker attempt centrally, inherits a fixed parent scope, locks household/document/batch FK references before tree authority, validates parent ownership/ancestry, and inserts the edge in that transaction. Explicitly different child scope fails before acquiring root authority. Existing queue workers already establish scope. Repeated same-document semantic/extraction fanout reuses its reference locks. Unscoped request and watched-folder root creation remains intentional. Future multi-document analysis needs a predeclared lock set before relaxing scope restrictions.

Every lifecycle transition takes the immutable tree-root transaction advisory lock before job-row locks. Claims and expired-job recovery use nonblocking roots and bounded candidate batches. Renew/publication recheck the DB wall clock after locks. Bulk cancellation locks all roots in UUID order. Cancellation/retry/recovery never acquire domain rows. Creation must prelock FK references; peer review caught and closed that inversion, with a separate-connection sibling document-lock/enqueue regression. Future desired-run/document fencing must preserve this ordering; this code does not establish document-run authority.

Children are not runnable until all ancestors succeeded and all captured parent generations match. Manual retry resets retry count but never execution generation. Failure/retry/lease recovery permanently revokes old descendants, so their direct retry cannot resurrect an old branch. Successful parent cancellation with descendants preserves prior succeeded history and marks lineage revoked; active descendants require includeRunning. Revoked descendants cannot publish, enqueue, renew or retry. JobState/OpenAPI and parse-debug API/Viewer label expose lineageRevokedAt. Repeated cancellation preserves the original revocation timestamp; already revoked descendants are not rewritten.

No current orchestration parent waits on queued descendants: semantic planning enqueues then completes; document extraction executes its plan inline. Future Qwen continuation fan-in must use explicit assembly dependencies, not keep a parent running while children require its success.

Validation: current combined units1302passed; focused mypy20files, Ruff and contract validation passed.15 jobownership+lineage SQL tests collect, with10newlineage cases covering parent success gating, completed-parent cancellation, ABAretry, independent parent-generation mismatch while child token remains live, enqueue/cancel both lock orders, renewal/recovery race, expired-parent recovery, sibling domain/root lock order, and repeated-child scope rejection. Root alone will run canonical isolated GPU DB validation after coherent snapshot.

Remaining JOB-02/X-02: desired processing-run/current source authority, provider-neutral immutable parse generations and historical structural/evidence retention, cross-commit result/terminal-job atomicity, exact parse/fact revision for embedding and relationship projections, run-level cancellation for independently queued roots, generation-aware index activation/rollback and durable fan-in. This batch does not make Docling mandatory or change the accepted Qwen-native architecture, models, or serving runtime.
