# Cerebrum

> OpenWolf's learning memory. Updated automatically as the AI learns from interactions.
> Do not edit manually unless correcting an error.
> Last updated: 2026-04-29

## User Preferences

<!-- How the user likes things done. Code style, tools, patterns, communication. -->

- For duplicate artifacts in `pro-merged-master-v1.2`, read the Markdown file by default. DOCX review is only needed when the user explicitly asks for layout/fidelity review or the Markdown file is missing/incomplete.
- For UI, implement pixel-for-pixel from the Figma mockups using Figma MCP plus Playwright validation. Stop and ask the user if UI/UX ambiguity remains after checking Figma frames, component variants, interaction specs, edge states, and redlines.
- First working Structura screen must be Inbox. UI priority order is: 1) upload + inbox + document viewer, 2) review queue + evidence inspector, 3) folder/tag filing workflow.
- After every local commit and push to GitHub, immediately SSH to the GPU node and pull/update `/tank/repos/structura`.
- Application virtualenvs for the GPU node belong under `/tank/venvs`, not inside the repo.
- Do not call Structura phase or major-milestone completion from Mac-only tests. Mac validation is preflight only; live/integration/runtime/Docker/model milestone evidence must be run on the GPU node after commit, push, SSH, and pull.
- Before creating any GPU-node directory or ZFS dataset, inspect the current node state first. Do not assume `/tank/repos`, `/tank/repos/structura`, `/tank/venvs`, `/srv/structura`, or any `tank/structura/*` dataset is missing or present without checking.
- Do not install or depend on host `node`/`npm` on the GPU node for Structura gates. Use pinned container/app images for web lint/build and Playwright so Node/npm versions are reproducible.
- Do not substitute backend integration tests or mocked Playwright screenshot tests for live browser coverage when a phase has a user-visible runtime workflow. The GPU live browser suite must include every implemented phase live spec before a phase/milestone is called complete.
- After completing a remediation or artifact-only pass, still commit, push, and pull to `/tank/repos/structura` when the result is project state future agents need. Documentation and UI-reference commits are part of the canonical baseline.
- Act as an architecture steward, not only a feature implementer. Preserve separation of concerns, SRP, high cohesion, low coupling, explicit layer boundaries, small understandable units, behavior-preserving refactors, meaningful abstractions, clear interfaces, and tests/type checks as guardrails.
- Before editing code, inspect target files for overloaded responsibilities. If a file is already accumulating unrelated routing, validation, persistence, orchestration, formatting, or UI logic, pause and extract or propose a focused refactor before adding more logic.
- Keep route/controller/UI code thin. Put business rules and orchestration in service/domain modules, database access in repositories/DAOs, external integrations in adapters, and reusable pure rules in precisely named modules rather than vague utilities.
- Treat file size as an architecture warning signal: review files approaching 300-500 lines; treat files over 500 lines as refactor candidates unless intentionally large; do not add logic to files over 800 lines without refactoring or explicitly justifying the exception.
- Avoid god files/classes, kitchen-sink `utils` modules, vague `manager`/`processor`/`helper` modules, business logic hidden in route handlers or UI components, random SQL spread through the codebase, circular imports, broad catch-all exception hiding, and boolean-flag explosions.
- When modifying oversized modules, prefer small behavior-preserving extraction before or alongside the requested change. Name new modules by responsibility, keep dependency direction clean, preserve public APIs where practical, and add or retain tests around moved behavior.
- Use `STRUCTURA_IMPLEMENTATION_PLAN.md` as the phase map and sequencing source of truth, but always pull in associated non-archive artifacts for implementation detail because the root plan is intentionally not comprehensive.
- When Structura artifacts exist in both Markdown and DOCX form, read the Markdown artifact by default; only inspect DOCX when the user explicitly asks for layout/fidelity review or when Markdown is missing/incomplete.
- For large artifact reviews, terminal output can truncate even when the read command succeeds. Verify length with `wc -l` and read bounded non-overlapping chunks with tools like `sed -n`, rather than broad combined `cat` calls.
- For Structura implementation work, start with the root plan, then the relevant phase-specific execution plan, then the subphase Fresh Context list. Re-read the relevant documents before coding each subphase rather than relying on old context.
- For APIs, contracts, conventions, library behavior, security, database semantics, UI testing, backup/restore, release engineering, or any uncertain implementation decision, use Firecrawl to gather current evidence from primary sources and record the rationale in implementation notes, ADRs, or release docs.
- For Phase 8.5, Qwen3-VL 8B must never be invoked automatically by application logic. It is allowed only when the user explicitly selects High Quality Parse, or when the user explicitly permits one bounded 8B rescue pass before human review.
- For Phase 8.5, do not call document uncertainty a failure. Low-confidence, ambiguous, unreconciled, or low-signal content should become `needs_human_review` or `insufficient_signal`; `pipeline_failed` is reserved for runtime/system defects.
- For Phase 8.5 live extraction work, treat the application database as part of the validation target. It is not enough for model output to look good in raw JSON; verify `document_extractions`, `field_candidates`, `line_item_candidates`, provenance, review status, and current-row scope in Postgres.
- Use the precise model name `Qwen3-VL-4B`, not the shorthand `Qwen4`, for the current Smart Parse semantic service.
- The external Codex native memory store is read-only under the active environment instructions. When the user asks to update native memory, update repo-local durable guidance (`agents.md` and `.wolf/*`) and state that external native memory could not be written from the workspace.
- The duplicate `docs/01_App_Specification` and `docs/02_Phased_Implementation_Plan` Markdown/DOCX pairs were spot-checked on 2026-04-24 and no material content differences were found; Markdown remains the default working source unless fidelity review or suspected drift requires opening DOCX.
- As of 2026-04-27, Structura is implemented through Phase 7 on `master`; local, `origin/master`, and GPU `/tank/repos/structura` must be synced before milestone validation. Phase 8 is the next implementation phase.
- Phase 2 scope is manual organization only: manual folders, smart-folder records, tags, document metadata edits, filing notes, multi-folder membership, primary folder selection, folder filtering, list/detail propagation, audit coverage, and Inbox/Viewer filing UI. Dynamic smart-folder execution, filing-rule automation, watched-folder ingestion, model filing suggestions, extraction review workflows, Docling parsing, and search ranking remain later-phase work.
- Phase 2 hardening closed confirmed gaps in cross-household job/admin visibility, dead-letter retry claimability, web proxy request/response buffering, preview-worker placeholder behavior, and incomplete folder/tag filing screenshot artifacts. Preserve those regression tests and seams when starting Phase 3.
- Phase 3 scope is canonical parse only: queued Docling conversion, immutable derived parse artifacts, relational pages/elements/tables/chunks, page preview asset refresh, admin-scoped parse-debug API, and Viewer parse-debug UI. Phase 4 classification, extraction, model gateway, candidate normalization, canonical promotion, review queue, and evidence-review UI are not Phase 3 scope.
- Keep Docling/Torch/OpenCV isolated to the dedicated `worker-docling` image. Do not add Docling to shared API/previews requirements or make API/previews depend on Docling imports; the API should enqueue parse work, not convert documents inline.
- Phase 3 Docling PDF conversion disables OCR by default, keeps table structure enabled, and routes Hugging Face/XDG/RapidOCR caches to `/srv/structura/cache`; OCR/model downloads must not write into Python site-packages.
- Phase 3 audit remediation added DB-centralized document read authorization through `document_is_readable(document_id, household_id, user_id, household_role)`. Document list/detail/assets and organization-write locking must use that predicate path rather than household-only filters.
- Folder-name uniqueness is household-scoped as of migrations `066_folder_household_uniqueness.sql` and `067_document_read_acl_function.sql`; do not reintroduce parent/name global uniqueness for household folders.
- Job workers rely on lease expiry recovery in `JobService.claim_next_job_record`; stale `running` jobs with expired leases are requeued or dead-lettered before new claims, and admin retry may only reset running jobs after lease expiry.
- Phase 2 folder/tag filing has a strict UI-reference artifact set under `docs/ui-reference/figma/folder-tag-filing/`: `figma-context.json`, `figma-screenshot.png`, `handoff-interaction-specs.png`, `handoff-edge-states.png`, `handoff-dev-redlines.png`, `extraction-workspace-reference.png`, `playwright-screenshot.png`, and `comparison-notes.md`.
- Phase 3 parse-debug has a strict UI-reference artifact set under `docs/ui-reference/figma/parse-debug/` with `figma-context.json`, `comparison-notes.md`, and `playwright-screenshot.png`; the Playwright baseline is `tests/e2e/phase3.spec.ts-snapshots/phase3-parse-debug-chromium-linux.png`.
- Phase 4 review-extraction has a strict UI-reference artifact set under `docs/ui-reference/figma/review-extraction/` with `figma-context.json`, `comparison-notes.md`, `figma-screenshot.png`, and `playwright-screenshot.png`; the Playwright baseline is `tests/e2e/phase4.spec.ts-snapshots/phase4-review-queue-chromium-linux.png`.
- Phase 5 search has a strict UI-reference artifact set under `docs/ui-reference/figma/search/` with `figma-context.json`, `comparison-notes.md`, `figma-screenshot.png`, and `playwright-screenshot.png`; the Playwright baseline is `tests/e2e/phase5.spec.ts-snapshots/phase5-corpus-search-chromium-linux.png`.
- Phase 6 automation has a UI-reference artifact set under `docs/ui-reference/figma/automation/` with `figma-context.json`, `comparison-notes.md`, and `playwright-screenshot.png`; the Playwright baseline is `tests/e2e/phase6.spec.ts-snapshots/phase6-automation-workbench-chromium-linux.png`. There is no single dedicated Figma frame for this surface; it follows the later-surface handoff/review workspace references.
- Phase 7 relationships/timelines has a UI-reference artifact set under `docs/ui-reference/figma/relationships-timelines/` with `figma-context.json`, `comparison-notes.md`, and `playwright-screenshot.png`; the Playwright baseline is `tests/e2e/phase7.spec.ts-snapshots/phase7-relationships-timeline-chromium-linux.png`.
- Phase 4 implementation code landed in commit `d04a762`: classification over canonical Docling parse text, deterministic Docling-text extraction gateway, receipt/invoice/EOB validators, field and line-item candidates, canonical fact promotion, review task/action APIs, `worker-extraction`, and Review Queue UI.
- Phase 3 live browser coverage was added in commit `a37c0c7`: `tests/e2e/phase3-live.spec.ts` uploads a generated valid PDF through the GPU-hosted web UI, waits for the live Docling worker to persist parse artifacts, and verifies Parse Debug in the Viewer. The helper `tests/e2e/support/pdf.ts` writes deterministic text-layer PDFs for live upload tests.
- Phase 5 started from `STRUCTURA_PHASE_5_IMPLEMENTATION_PLAN.md` and consumed the Phase 4 seams: accepted canonical facts in `canonical_fields`/`canonical_line_items`, document rollups for counterparty/date/amount filters, refreshed `document_chunks.bm25_text` projection from accepted facts, immutable derived extraction artifacts, current asset supersession on rerun, and review/audit history for human-confirmed facts.
- Preserve the Phase 5 architecture guardrail: do not append search/embedding/ranking behavior into document routes, review routes, or extraction modules. Keep cohesive search/retrieval services, repositories, and workers; keep route handlers thin and model/provider behavior behind adapters.
- Phase 5 search is implemented with dedicated `lib/search/` modules, `apps/api/structura_api/routes_search.py`, `workers/embeddings/worker.py`, `database/069_phase5_search.sql`, and `apps/web/src/components/SearchResults.tsx`. Preserve these module boundaries; do not move contacts, watched-folder intake, or filing-rule orchestration into search modules during Phase 6.
- Phase 5 hardening landed in commit `81abea2`: smart-folder saved-query parsing now lives in `lib/search/saved_query.py`, document list/smart-folder SQL lives in `lib/documents/list_repository.py`, document summary mapping lives in `lib/documents/summary_mapping.py`, Phase 5 UI filter-state construction lives in `apps/web/src/components/SearchFilterPanel.tsx`, and `workers/ingest/worker.py` replaced the ingest placeholder.
- Phase 6 should reuse `lib/search/projection.py::refresh_projection_and_enqueue_embedding` after filing/contact/rule mutations that change searchable document metadata. Search ACLs remain centralized through `document_is_readable`; smart folders parse saved-query JSON into canonical `SearchFilters` and resolve through the same `document_filter_sql` predicates as `/api/v1/search`. `document_matches_saved_query` remains a database guardrail/compatibility helper, not the primary planner.
- The former Phase 6 placeholder POST endpoints for contacts, filing rules, and watched folders were replaced by real Phase 6 implementations. Future-phase placeholders such as relationships, analysis notes, and exports must advertise and return `501` until the owning phase implements them, and runtime/OpenAPI/parity tests must change together when they become real.
- Phase 5 canonical validation after hardening was completed on the GPU node at commit `81abea2`: migration `071_phase5_search_guardrails.sql` applied; `api`, `web`, and `worker-ingest` rebuilt/restarted; `ruff`, format, contracts, `pyright`, `mypy`, live-DB pytest (`73 passed`), `make sast`, pinned Node web lint/build, Compose health, and live Playwright phase1-5 (`5 passed`) all succeeded.
- The canonical live browser command after Phase 7 includes all implemented phase specs: `STRUCTURA_E2E_LIVE=1 STRUCTURA_E2E_WEB_URL=http://10.25.0.50:13000 npx playwright test tests/e2e/phase1-live.spec.ts tests/e2e/phase2-live.spec.ts tests/e2e/phase3-live.spec.ts tests/e2e/phase4-live.spec.ts tests/e2e/phase5-live.spec.ts tests/e2e/phase6-live.spec.ts tests/e2e/phase7-live.spec.ts --workers=1`.
- Phase 6 automation is implemented in cohesive modules: contacts in `lib/contacts`, filing-rule validation/evaluation/orchestration in `lib/automation/rule_policy.py`, `rule_engine.py`, `repository.py`, `service.py`, and `action_application.py`, watched-folder policy/persistence/service in `lib/automation/watched_folder_policy.py`, `watched_folder_repository.py`, and `watched_folders.py`, shared document ingestion in `lib/documents/ingestion.py`, organization mutation in `lib/organization/document_organization.py`, operator maintenance enqueues in `lib/documents/maintenance.py`, and watched-folder intake in `workers/watched_folders/worker.py`.
- Phase 6 rule application must use `lib/automation/action_application.py` with a caller-supplied cursor and `lib/organization/document_organization.py` for manual-folder/tag mutations. This keeps folder/tag/metadata/review-task changes, filing-rule runs, and suggestion decisions transactionally atomic while preserving folder ACL, primary-folder, audit, and projection-refresh semantics.
- Phase 6 watched-folder intake imports only stable PDFs, rejects/ignores non-PDF candidates, blocks managed Structura runtime directories, enforces the configured allowed root (`STRUCTURA_WATCHED_FOLDER_ROOT`, default `/srv/structura/imports`), rejects symlinked candidate files, and routes accepted files through shared document ingestion. Do not let relationship extraction, timeline building, or non-PDF intake creep into `worker-watched-folders`; those are later-phase concerns unless explicitly changed.
- Phase 6 filing suggestions use `filing_rule_runs.decision_status` for `pending`, `accepted`, `rejected`, and `deferred`. Phase 7 relationship suggestions should use relationship-specific persistence/workers rather than overloading filing-rule run state.
- Phase 6 operator CLI maintenance commands enqueue normal jobs through `lib/documents/maintenance.py`; `bulk-import --execute` posts stable PDFs through the authenticated `/api/v1/documents` upload API with an explicit `X-API-Token`. Do not reintroduce placeholder HTTP checks or direct downstream table mutation for reprocess/rebuild operations.
- Phase 6 remediation landed in commits `45193ba` and `7111b83` and was GPU-validated at current HEAD with `90 passed` live-DB pytest, SAST/type gates, pinned-container web lint/build, Compose health, and live Playwright phase1-6 against `http://10.25.0.50:13000`.
- Phase 7 relationships are implemented in cohesive modules: `apps/api/structura_api/routes_relationships.py`, `lib/relationships/relationship_repository.py`, `deadline_repository.py`, `timeline_repository.py`, `repository.py`, `service.py`, `suggestions.py`, `jobs.py`, `workers/relationships/worker.py`, `database/073_phase7_relationships.sql`, `apps/web/src/components/RelationshipPanel.tsx`, and `RelationshipWorkspace.tsx`. Keep relationship graph/timeline/deadline code in these modules rather than appending it to document/search/review/automation routes.
- Phase 7 hardening decisions: deadline refresh joins `field_candidates` for selected-candidate confidence rather than reading `canonical_fields.confidence`; relationship ACL requires both linked documents to be readable, and write decisions require writable access to both documents; relationship persistence was split below god-module thresholds; live Playwright selectors must be scoped for shared GPU DB duplicate labels; integration tests that enqueue durable jobs must tolerate live Compose workers claiming those jobs.
- Phase 7 was GPU-validated at commit `eebdb3d` with migration `073_phase7_relationships.sql`, rebuilt `api`/`web`/worker profiles including `relationships`, live-DB pytest (`97 passed`), SAST/type gates, pinned-container web lint/build, Compose health, and live Playwright phase1-7 (`7 passed`) against `http://10.25.0.50:13000`.
- Phase 8 should start from the Phase 7 seams: text search remains the default retrieval path, relationship/deadline filters flow through canonical `SearchFilters`/saved-query planner parity, accepted canonical date fields feed deadlines, relationship suggestions/decisions stay separate from filing-rule runs, `worker-relationships` is a real queue consumer, and visual/handwriting retrieval must be added as separate modules/workers without folding into relationships, extraction, or text embeddings.
- The Figma source for Phase 2 folder/tag filing is composite, not a single dedicated finalized filing frame: primary `17:2`, Viewer propagation `14:434`, future extraction workspace reference `14:611`, handoff interaction specs `35:7`, edge states `35:12`, and dev redlines `35:17`. The older filing-rules/watched-folders mockups are deferred automation scope.
- Phase 8.5 critical extraction closure is implemented through commit `9fd1534`: scoped `document_extractions` persistence, Granite model-output schemas, semantic-type-driven Granite routing, model-output normalization, and aggregate invoice reconciliation. The closure files are `database/078_phase8_5_region_extraction_scope.sql`, `contracts/model_outputs/*`, `lib/extraction/model_output_schemas.py`, `lib/extraction/granite_prompting.py`, `lib/extraction/model_output_normalization.py`, `lib/extraction/reconciliation.py`, and `lib/extraction/reconciliation_repository.py`.
- Phase 8.5 Granite output should not be treated as canonical app JSON. Granite emits task-specific model-output shapes; Structura owns normalization into canonical invoice/EOB candidates, records repairs/rejected fields in `normalization_json`, runs validators, and keeps model-backed aggregates in `needs_review`.
- Phase 8.5 semantic type can be more reliable than Qwen's `granite_task` label. If Qwen returns `semantic_type=invoice_line_item_table` or `covered_services_line_item_table` with `granite_task=kvp`, route Granite through the table/line-item model-output schema anyway.
- Phase 8.5 BMW live validation proved the line-item fix: standard live run at `9fd1534` on document `6f6bd028-7e9c-42aa-a5c3-84cae29da655` produced 3 current Granite invoice line-item region extractions, 4 Granite region line-item candidates, 4 aggregate line-item candidates, current aggregate invoice JSON with invoice number `6046058/1`, zero failed/dead-letter jobs, and zero Qwen8B invocations.
- Phase 8.5 Qwen3-VL-4B Smart Parse should reuse the existing Docling-grounded semantic harness/contracts from the 2B path. The active default now attempts four page images per semantic request to match the old 2B fan-in shape, keeps exact Docling page coverage mandatory, and falls back to one-page windows only when multi-image output omits a Docling page.

## Key Learnings

- **Project:** structura
- Canonical working docs at repo root are `STRUCTURA_PLAN_INDEX.md`, `STRUCTURA_IMPLEMENTATION_PLAN.md`, and `STRUCTURA_UI_FIGMA_QA_PLAN.md`. Use these together with the original artifacts during implementation.
- Source artifact pack lives at `/Users/brennanconley/vibecode/structura/pro-merged-master-v1.2`. Each implementation phase in `STRUCTURA_IMPLEMENTATION_PLAN.md` has required artifact paths that must be reviewed before coding that phase.
- For implementation depth, use related non-archive artifact-pack docs, contracts, database SQL, and infrastructure files alongside the root implementation plan. Do not inspect or rely on `archive/`.
- Figma file key is `5GAPHbduQLu9INBOXUPxJN`. User-provided Figma nodes `14:2` and `35:2` are page nodes, not direct screen frames. Use concrete frames including `17:2`, `14:434`, `14:611`, `14:797`, `14:990`, plus handoff pages `35:2`, `35:7`, `35:12`, and `35:17`.
- GitHub remote is `https://github.com/bgconley/structura.git`; current branch is `master`.
- GPU node sync target is `bgconley@10.25.0.50` using SSH key `/Users/brennanconley/vibecode/infx/ubuntu24_ed25519`; repo checkout path is `/tank/repos/structura`.
- GPU node source/venv paths are node-specific: clone or pull the repo at `/tank/repos/structura`, and create application virtualenvs under `/tank/venvs`.
- On 2026-04-25, direct GPU-node inspection showed `tank/repos` exists and is mounted at `/tank/repos`; `/tank/repos/structura` exists as a checkout directory; `/tank/venvs` exists but is on the root ext4 filesystem, not a dedicated ZFS dataset; `/srv/structura` does not exist; Docker root is `/var/lib/docker` on root ext4.
- On 2026-04-25, none of the expected `tank/structura` runtime datasets existed: parent `tank/structura`, `postgres`, `redis`, `objects-canonical`, `objects-derived`, `objects-exports`, `models`, `staging`, `cache`, `repo`, `venv`, `config`, `logs`, `backups`, `observability`, or `tmp`.
- Structura runtime data and Compose bind mounts use the ZFS runtime root `/srv/structura`, not the `/tank/repos` checkout. Key mounts are `/srv/structura/postgres`, `/srv/structura/redis`, `/srv/structura/objects/canonical`, `/srv/structura/objects/derived`, `/srv/structura/objects/exports`, `/srv/structura/models`, `/srv/structura/staging`, `/srv/structura/cache`, `/srv/structura/config`, `/srv/structura/logs`, `/srv/structura/backups`, `/srv/structura/observability`, and `/srv/structura/tmp`.
- Model storage belongs at `/srv/structura/models`; model service logs belong at `/srv/structura/logs/models`.
- The artifact set does not specify a Docker daemon image-store/data-root. Do not move Docker image storage or invent a path without an explicit decision/ADR; use the GPU node's existing Docker configuration until then.
- GPU-node host tooling should stay minimal: orchestration tools, Docker/Compose, ZFS, Git/SSH, and Python venvs for Python gates. Web build tooling is containerized; current pinned images are `node:20-alpine` for the app web image and `mcr.microsoft.com/playwright:v1.59.1-noble` for browser E2E.
- Playwright milestone validation must use the GPU-hosted web app over the LAN. Run live browser tests with `STRUCTURA_E2E_LIVE=1` targeting `http://10.25.0.50:13000`; do not start Mac-hosted Vite for phase completion. The GPU Compose `.env` should expose only web (`STRUCTURA_WEB_BIND_HOST=0.0.0.0`, `STRUCTURA_WEB_PORT=13000`) and keep API/Postgres loopback-only.
- Phase 0A-0F is implemented locally. The next build slice should start at Phase 1 in `STRUCTURA_IMPLEMENTATION_PLAN.md`: upload, Inbox, and protected viewer.
- Dedicated execution plans now exist for Phase 1, Phase 2, Phase 3, Phase 4, Phase 5, Phase 6, Phase 7, Phase 8, Phase 9, Phase 10, Phase 11, and the final derived Phase 12 at `STRUCTURA_PHASE_1_IMPLEMENTATION_PLAN.md`, `STRUCTURA_PHASE_2_IMPLEMENTATION_PLAN.md`, `STRUCTURA_PHASE_3_IMPLEMENTATION_PLAN.md`, `STRUCTURA_PHASE_4_IMPLEMENTATION_PLAN.md`, `STRUCTURA_PHASE_5_IMPLEMENTATION_PLAN.md`, `STRUCTURA_PHASE_6_IMPLEMENTATION_PLAN.md`, `STRUCTURA_PHASE_7_IMPLEMENTATION_PLAN.md`, `STRUCTURA_PHASE_8_IMPLEMENTATION_PLAN.md`, `STRUCTURA_PHASE_9_IMPLEMENTATION_PLAN.md`, `STRUCTURA_PHASE_10_IMPLEMENTATION_PLAN.md`, `STRUCTURA_PHASE_11_IMPLEMENTATION_PLAN.md`, and `STRUCTURA_PHASE_12_IMPLEMENTATION_PLAN.md`; use them as phase-specific implementation guides after the root plan. The root implementation plan stops at Phase 11; Phase 12 is the final internal-GA/operator-handoff phase derived from the release-candidate and definition-of-done artifacts.
- Phase plan usage order for implementation:
  1. Read `STRUCTURA_PLAN_INDEX.md` for source alignment, UI/Figma rules, GPU sync, and stop rules.
  2. Read `STRUCTURA_IMPLEMENTATION_PLAN.md` for sequencing, phase gate, and required artifact list.
  3. Read the matching `STRUCTURA_PHASE_N_IMPLEMENTATION_PLAN.md` for subphase execution detail.
  4. Re-read the subphase's Fresh Context files immediately before coding that subphase.
  5. Pull in the associated non-archive artifact-pack docs, contracts, database SQL, infrastructure files, and UI references named by the plan.
  6. If implementation touches UI, inspect the concrete Figma frame and run/record Playwright validation.
  7. If implementation touches API/schema/database/events, keep OpenAPI, JSON Schemas, DTOs, route parity, migrations, tests, and docs synchronized in the same change set.
  8. Verify with the phase gate commands and targeted tests before moving on.
- Phase 11 is release-candidate measurement and regression: golden corpus, expected answers, deterministic evaluation, extraction/search scoring, Playwright smoke, migration-from-scratch, restore rehearsal, SAST/data-flow, performance, and evidence pack. It should not be used as a broad feature expansion phase.
- Phase 12 is the final derived internal-GA/operator-handoff phase: consume Phase 11 evidence, close valid blockers, freeze contracts/schema/runtime assumptions, finalize runbooks and release notes, prepare tag/deployment sync if requested, write go/no-go, and stop. Do not create Phase 13 or continue into new feature work without explicit user direction.
- When findings arise in review or validation, map each finding back to the owning phase plan and artifacts before fixing it. Do not resolve it from memory alone.
- Phase-specific plans are execution guides, not replacements for the artifact pack. If the root plan, phase plan, Figma, contracts, database SQL, or artifacts disagree, follow the source alignment policy in `STRUCTURA_PLAN_INDEX.md` and stop for user clarification when the conflict is material.
- The root planning docs were aligned on 2026-04-24 to match the phase-specific plans: Markdown-first for duplicate artifact pairs, with DOCX reserved for fidelity review, suspected incompleteness, or suspected material mismatch.
- Phase 8.5 canonical semantic pipeline is Docling physical parse -> Qwen3-VL-4B semantic annotation -> Granite 4.0 3B Vision targeted extraction -> validators/provenance/human review -> canonical facts/search/evidence. Docling owns physical truth, Qwen owns planning semantics, Granite owns structured extraction, and validators/review own truth gating.
- Phase 8.5 model availability is not model authorization. Qwen3-VL 8B may be loaded or co-resident for capacity reasons, but it cannot be invoked unless the user selected High Quality Parse or explicitly allowed one 8B rescue pass.
- Phase 8.5 corpus and live validation must preserve mode semantics. Standard corpus runs should not secretly force HQ/rescue; HQ and rescue lanes need explicit separate evidence.

## Do-Not-Repeat

<!-- Mistakes made and corrected. Each entry prevents the same mistake recurring. -->
<!-- Format: [YYYY-MM-DD] Description of what went wrong and what to do instead. -->

- [2026-04-24] Do not call Figma design-context tools on page nodes like `14:2` or `35:2` and assume they are screen frames. Inspect the Figma page structure first and target concrete child frames.
- [2026-04-24] Do not assume a large `cat` output was fully delivered into agent context. Use file length checks and explicit chunked reads so full artifact coverage is auditable.
- [2026-04-24] Do not treat the phase-specific plans as permission to skip artifact rereads. Each subphase explicitly requires fresh context, especially around API contracts, schema, database, UI, security, backup/restore, and release gates.
- [2026-04-24] Do not invent phases after Phase 12. The root implementation plan stops at Phase 11, and Phase 12 is the final derived internal-GA/operator-handoff plan unless the user explicitly asks for new planning.
- [2026-04-24] Do not force duplicate DOCX review when the corresponding Markdown file has already been parity-checked and no material difference is known. Use DOCX only for fidelity review, missing/incomplete Markdown, or suspected drift.
- [2026-04-25] Do not run live/integration/runtime milestone gates only on the Mac and report them as complete. Commit, push, pull to `bgconley@10.25.0.50:/tank/repos/structura`, then build and test on the GPU node for completion evidence.
- [2026-04-25] Do not say or imply `/tank/repos` needs to be created without inspecting the GPU node first. It already exists as `tank/repos`; future creation decisions must follow current `zfs list`/`findmnt` evidence.
- [2026-04-25] Do not install host Node/npm on the GPU node as a workaround for web gates. Containerize Node-dependent verification with pinned images instead.
- [2026-04-25] Do not keep appending feature logic to oversized Structura modules. `apps/api/structura_api/routes_documents.py` grew past 800 lines during Phase 2 kickoff and must be decomposed before more organization behavior is added there.
- [2026-04-26] Do not claim strict Figma artifact completeness from Playwright snapshots alone. For implemented UI slices, maintain the `docs/ui-reference/figma/<screen>/` set with Figma context, source screenshot(s), Playwright comparison screenshot, and comparison notes.
- [2026-04-26] Do not use the filing-rules/watched-folders mockup as the Phase 2 manual filing baseline. It represents later automation scope; Phase 2 filing is governed by the Inbox/Viewer composite source set and handoff frames.
- [2026-04-26] Do not put Docling/Torch into the shared API or preview worker Python requirements. A GPU Compose build showed that doing so bloats every backend image; Docling belongs only in `worker-docling`.
- [2026-04-26] Do not assume Python package installation is enough for Docling. The worker image also needs native OpenCV/PDF pipeline libraries such as `libxcb1`, `libgl1`, `libglib2.0-0`, and related X/OpenMP runtime libs.
- [2026-04-26] Do not implement document/asset authorization by household filter alone. Use the central `document_is_readable` predicate so folder ACL, document ACL mode, sensitivity, owner, and owner/admin access are applied consistently.
- [2026-04-26] Do not repair Bandit SQL-construction findings by suppressing B608 when a static schema-level predicate can keep both security tooling and architecture clean.
- [2026-04-26] Do not treat parse-debug as the Phase 4 review UI. Parse-debug is an admin diagnostic extension; Phase 4 review queue, candidate comparison, correction, and evidence-jump surfaces need their own implementation and UI-reference artifacts.
- [2026-04-26] Do not insert new current extraction assets on rerun without first superseding old current `raw_model_output` and `normalized_extraction_json` assets. The baseline has a one-current-asset unique index per document/role/page, so reruns must preserve that invariant.
- [2026-04-26] Do not omit an already-implemented phase from the GPU live Playwright suite. Phase 3 originally had backend integration and mocked screenshot coverage but no `phase3-live.spec.ts`; every UI/runtime phase must have a live spec or an explicit documented exception before completion claims.
- [2026-04-26] Do not generate a UI screenshot assertion after navigating away from the target surface. Phase 5 initially snapshotted after evidence jump; snapshot assertions must capture the intended screen before navigation.
- [2026-04-26] Do not make live Playwright locators page-wide when the shared GPU database can contain duplicate labels from previous runs. Use unique markers and scope actions to the target row/card before clicking buttons such as `Jump to evidence`.
- [2026-04-26] Do not let later-phase job volume hide earlier-phase diagnostic proof. The Viewer Parse Debug panel must keep `docling_convert` visible even after classify/extract/embed jobs are added, because Phase 3 live validation depends on the canonical parse job status being inspectable.
- [2026-04-26] Do not expose BM25/ParadeDB highlight markup as literal client text. Normalize search snippets at the service/API boundary, and avoid asserting exact hyphenated query strings inside rendered evidence snippets because search highlighting can tokenize terms.
- [2026-04-26] Do not let smart-folder saved-query execution drift from `/api/v1/search` planner semantics. Unsupported saved-query keys must be rejected at write time, smart-folder document lists must use the shared `SearchFilters`/`document_filter_sql` path, and the database saved-query function must remain a guardrail rather than a divergent second planner.
- [2026-04-26] Do not enqueue jobs to a placeholder-only runtime worker. If upload/API code creates queue work, the matching Compose service must claim/complete/fail that queue or the API must stop enqueueing until a worker exists.
- [2026-04-26] Do not let future-phase placeholder OpenAPI contracts advertise successful mutation responses while runtime returns `501`. Placeholder endpoints must expose placeholder status codes until the owning phase implements them.
- [2026-04-26] Do not fix Phase 5 filter breadth by stuffing more state into `SearchResults.tsx` or document detail read models. Keep search UI filter state in `SearchFilterPanel.tsx`, document list query behavior in `lib/documents/list_repository.py`, and summary mapping in `lib/documents/summary_mapping.py`.
- [2026-04-26] Do not use broad URL-open helpers in operator CLIs when a local job-enqueue path exists. Phase 6 SAST correctly flagged `urllib.request.urlopen`; maintenance commands should enqueue normal jobs through typed local services instead of placeholder HTTP checks.
- [2026-04-26] Do not mount the repo directly into a Linux Playwright container and run `npm ci` against the host `node_modules`. Generate Linux screenshots from a container-local copy and copy only the intended snapshot/reference artifact back into the repo.
- [2026-04-26] Do not let filing-rule policy/contracts advertise action types that the apply path silently skips. Every allowed action must have execution and integration coverage, or it must be removed from the allowed set until implemented.
- [2026-04-26] Do not call independently committing organization/manual-filing services from automation apply/accept flows before writing filing-rule run/audit state. Use cursor-based action application so document mutations and automation audit state commit or roll back atomically.
- [2026-04-26] Do not allow watched-folder configuration to accept arbitrary existing absolute directories. Validate against configured intake roots and reject symlinked candidate files before import.
- [2026-04-26] Do not rely on Mac host `npm` after Linux container gates have written or pruned `node_modules`. Host optional-dependency state can be inconsistent; use pinned container web gates for Structura milestone evidence.
- [2026-04-27] Do not read non-existent confidence from `canonical_fields`. Deadline refresh should join through `selected_candidate_id` to `field_candidates.confidence` and preserve null confidence for human/system canonical fields.
- [2026-04-27] Do not leave new Phase 7 persistence in a 700-line repository module. Keep relationship rows/mutations, deadline rows/mutations, and timeline/smart-view projections in separate cohesive repositories with a small compatibility facade.
- [2026-04-27] Do not assume live integration tests exclusively own durable jobs while the GPU Compose stack is running. If a test enqueues a job, drain opportunistically and then wait for terminal status because the live worker may claim it first.
- [2026-04-28] Do not let Phase 8.5 regress to fake model behavior. Fixture adapters may exist only behind `STRUCTURA_MODEL_MODE=fixture`; live/required mode must call Qwen, Granite, text embedding, and visual embedding adapters and persist source provenance only after successful adapter invocation.
- [2026-04-28] Do not load page image bytes in extraction repositories. Repositories should expose page asset URI/MIME/hash metadata; vision gateways own the I/O boundary when they actually need model inputs.
- [2026-04-28] Do not tune Phase 8.5 Blackwell model services by trial-and-error alone. For voipmonitor/cu130 vLLM on SM120, use Firecrawl-backed primary/fork sources before changing placement, memory, KV-cache, or quantization settings; container health without real inference is not enough evidence.
- [2026-04-28] Do not treat all Phase 8.5 live model surfaces as safely co-resident on the two 24GB Blackwell cards. GPU validation showed vLLM KV-cache exhaustion when multiple Qwen services shared one card and when too many services competed without bounded KV/cache settings. The current active profile is Qwen3-VL-4B semantic on GPU0, Granite on GPU1, and visual embedding co-resident with Granite only under the measured profile; Qwen3-VL 8B HQ/rescue is disabled/deferred.
- [2026-04-28] Do not request down-projected `dimensions` from Qwen3-VL-Embedding 2B unless a serving backend explicitly proves matryoshka support. The live voipmonitor/cu130 vLLM endpoint rejects the `dimensions` parameter and returns native 2048-dimensional vectors, so Structura visual embedding defaults, profile names, validation, and pgvector indexes must use 2048 dimensions.
- [2026-04-29] Awareness only, not an immediate priority: pgvector `vector` ANN indexes are capped below native 2048-dimensional visual embeddings, but the deployed pgvector 0.8.1 can store 2048-dimensional `vector` values and Structura indexes/searches those rows through `embedding::halfvec(2048)`. Keep this distinction clear if visual embedding/profile drift resurfaces; the known cleanup is profile-name/config alignment, not emergency dimensional down-projection.
- [2026-04-28] Do not iterate blindly on Phase 8.5 prompts, JSON schemas, normalizers, or vLLM flags without rechecking the intended Docling -> Qwen3-VL-4B -> Granite pipeline and the serving/runtime failure mode. Fix policy and orchestration drift before tuning output cleanup.
- [2026-04-28] Do not let Qwen3-VL 8B rescue fire from application logic, `validation.needs_review`, low confidence, high-risk document type, or private-corpus defaults. It requires explicit user permission and must be bounded to one pass before human review.
- [2026-04-28] Do not describe difficult, ambiguous, or low-signal documents as pipeline failures. If the system and models executed correctly, route them to `needs_human_review`, `insufficient_signal`, or `no_extraction_target`; reserve failure states for operational defects.
- [2026-04-29] Do not let a later region extraction supersede useful earlier region output. Phase 8.5 invoice/service-line regions and payment/header regions need separate current-row scopes plus aggregate reconciliation; a payment summary or header region must not erase service line candidates.
- [2026-04-29] Do not trust raw model-output shape or Qwen's `granite_task` label more than the semantic target. BMW live validation showed useful service-line data can arrive under `data.invoice_line_items` while Qwen labels the line-item region as `kvp`; force line-item schemas from semantic type and normalize bounded noncanonical shapes.
- [2026-04-29] Do not call the BMW Phase 8.5 closure complete from runner summaries alone. The proof requires database checks for region extraction rows, aggregate rows, line-item candidates, validation/review status, failed jobs, and Qwen8B provenance.
- [2026-04-29] Do not weaken Phase 8.5 semantic manifest validation when Qwen3-VL-4B omits page annotations. Keep the same contracts as the 2B smart path and adjust runtime/page-window fan-in so each Docling page remains covered exactly.
- [2026-04-29] Do not call Qwen3-VL-4B "Qwen4" in Structura docs, tests, or status updates; the precise model family/version matters for provenance and future runtime comparisons.
- [2026-04-29] Do not let isolated page-window context drive document-family routing. Qwen fallback prompts must carry whole-document Docling title/page-outline/table/anchor context, and merged document type must use page votes plus Docling anchors rather than first non-empty page output.
- [2026-04-29] Do not send Granite invoice, receipt, or medical EOB jobs solely because Qwen emitted a matching semantic type. Apply schema-fit gates using Docling anchors and downgrade unsupported or conflicting evidence to `document_observation`.
- [2026-04-29] Do not mix vLLM structured-output mechanisms in one request. Use one of `response_format: json_schema` or `structured_outputs.json`, keep prompt-level schema instructions, and retain JSON-object fallback with telemetry.
- [2026-04-29] Do not bypass Structura storage permissions for host-side Phase 8.5 diagnostics. Runtime object stores must preserve container UID write access and host operator group read/traverse access: writable dirs should be setgid and stored blobs group-readable, with existing GPU runtime trees repaired by `infrastructure/zfs/create_gpu_runtime_datasets.sh`.
- [2026-04-29] Do not make the GPU ZFS bootstrap remount live datasets just to repair permissions. Existing dataset properties should be checked before setting, and mounted datasets should not be remounted when they are already mounted.
- [2026-04-29] Do not quote shell globs in ZFS dataset names. Prefer `zfs list -r -d 1 <dataset>` for child dataset reporting so bootstrap scripts exit cleanly after repairs.
- [2026-04-29] Do not rely on setgid directories alone when the container UID is not in the host operator group. Structura app/worker containers need `STRUCTURA_RUNTIME_HOST_GID` via Compose `group_add` so UID 10001 can preserve setgid inheritance on `/srv/structura` bind mounts.
- [2026-04-29] Do not treat a one-token 16K vLLM rejection as a reason to shrink Qwen3-VL-4B semantic output below the existing contract. The measured Smart Parse profile can use a 24K context with the 3,840-token semantic output budget, while still falling back from multi-image to single-page windows when vLLM reports a context-length rejection.

## Decision Log

<!-- Significant technical decisions with rationale. Why X was chosen over Y. -->

- [2026-04-24] Root planning docs are the canonical working implementation layer for agentic coding, but they must be used alongside the original artifact pack. Phase-specific artifact lists in `STRUCTURA_IMPLEMENTATION_PLAN.md` are mandatory required context, not optional references.
- [2026-04-24] Implementation sequencing follows `STRUCTURA_IMPLEMENTATION_PLAN.md`; technical acceptance details come from the related non-archive artifacts, especially v1.3 ADR/normalization docs, contracts, database SQL, and infrastructure matrices. If the root plan is thin, expand from those artifacts rather than improvising.
- [2026-04-24] Git repo initialized in `/Users/brennanconley/vibecode/structura`, remote set to `https://github.com/bgconley/structura.git`, and `archive/` is ignored. Everything else requested by the user was tracked and pushed.
- [2026-04-24] Deployment development workflow targets GPU node `10.25.0.50`: after each commit/push, pull or clone to `/tank/repos/structura`; put venvs under `/tank/venvs`; otherwise follow artifact ZFS plan.
- [2026-04-25] Runtime placement is split by concern: source checkout is `/tank/repos/structura`, virtualenvs are `/tank/venvs`, durable app data is under `/srv/structura`, and model weights are under `/srv/structura/models`. Docker bind mounts come from `${STRUCTURA_RUNTIME_ROOT:-/srv/structura}`. Docker daemon image storage is unspecified in current artifacts and requires an explicit decision before changing.
- [2026-04-25] GPU-node ZFS preflight found existing pool `tank` online with `tank/repos` mounted at `/tank/repos`, but no `tank/structura` runtime dataset tree. Before production-equivalent Structura validation, create or map the missing `/srv/structura` runtime datasets intentionally; do not use Mac or root ext4 runtime state as completion evidence.
- [2026-04-25] Structura web verification should not depend on host Node/npm. Runtime and test Node versions are controlled by pinned container images: app web image `node:20-alpine`, browser E2E image `mcr.microsoft.com/playwright:v1.59.1-noble`.
- [2026-04-25] Architecture stewardship is a standing implementation requirement. New work should leave the codebase more modular, cohesive, testable, and less likely to collapse into god files. Complete features should be delivered through thin outer layers, cohesive services/repositories/adapters, and targeted tests rather than convenience-driven accumulation.
- [2026-04-26] Phase 2 recovery decisions: job/admin reads are household scoped with admin role/scope enforcement; manual dead-letter retry resets attempt state so the job is claimable; preview generation is queue-worker driven rather than inline upload work; and the web API proxy streams request/response bodies instead of buffering them.
- [2026-04-26] Phase 2 UI evidence decision: folder/tag filing is documented as a composite Figma source set and committed under `docs/ui-reference/figma/folder-tag-filing/` with deterministic Playwright snapshot assertions. A future UI change must update both the implementation and this evidence set deliberately.
- [2026-04-26] Phase 3 parse decision: uploads enqueue `docling_convert` jobs and the dedicated Docling worker owns real conversion, derived artifact persistence, page/element/table/chunk replacement, document parse metadata updates, and preview refresh. The API route remains a thin ingestion/enqueue boundary.
- [2026-04-26] Phase 3/4 seam decision: Phase 3 persists evidence-friendly pages, elements, tables, chunks, bbox/source refs, current Docling asset IDs, and protected parse-debug reads, but intentionally stops before Phase 4 classification/extraction/review-candidate behavior.
- [2026-04-26] Phase 3 Docling runtime decision: configure `PdfPipelineOptions` explicitly instead of relying on package defaults. OCR is off by default for deterministic digital-PDF conversion; cache paths are mounted under `/srv/structura/cache` for later OCR/model enablement.
- [2026-04-26] Phase 3 audit remediation decision: document read ACL is centralized as a Postgres function rather than copied into route/service SQL. This keeps application queries static for SAST, applies one predicate to list/detail/assets/organization writes, and leaves Phase 4 extraction/review work with a clean authorization seam.
- [2026-04-26] Phase 3 UI gate decision: parse-debug is treated as a Viewer diagnostic extension. The Phase 3 browser gate asserts the loaded parse-debug screenshot, and Phase 1/2 Viewer/Filing baselines were refreshed because the unloaded Phase 3 panel is now part of the current Viewer surface.
- [2026-04-26] Phase 4 readiness decision: begin Phase 4 from synchronized commit `5fc1587`. The Phase 4 work should build on existing parse rows, candidate/canonical/review tables, evidence contracts, hardened jobs, and `document_is_readable`; remaining work is implementation, not seam repair.
- [2026-04-26] Phase 4 extraction/review decision: keep classification, heuristic extraction, evidence resolution, validation, persistence, review actions, and UI in cohesive `lib/extraction`, `lib/review`, `workers/extraction`, and Review Queue modules rather than appending to document routes. Current raw/normalized extraction assets are superseded before reruns, canonical fact rollups are idempotent, and accepted facts refresh the document chunk projection for Phase 5.
- [2026-04-26] Phase 5 seam decision: Phase 4 intentionally stops at canonical fact production/review and does not implement retrieval. Phase 5 should consume accepted canonical facts, document rollups, and `document_chunks.bm25_text` projection through dedicated search/retrieval modules and workers.
- [2026-04-26] Live browser gate decision: milestone Playwright validation is phase-complete only when the command includes all implemented live specs. After Phase 4, the live suite is Phase 1 upload/viewer, Phase 2 filing, Phase 3 Docling parse-debug, and Phase 4 review queue.
- [2026-04-26] Phase 5 search decision: use deterministic local fixture embeddings as the default Phase 5 text embedding adapter so semantic/hybrid retrieval is testable without adding an external model-service dependency; reserve provider/model replacement behind `lib/search/embedding_gateway.py`.
- [2026-04-26] Phase 5 architecture decision: keep search parser, repository SQL, embedding persistence, embedding worker, saved-search persistence, and UI surface in separate cohesive modules. Phase 6 contacts/rules/watched folders must integrate through projection refresh and saved-query/search APIs rather than modifying document/review/extraction routes.
- [2026-04-26] Phase 5 hardening decision: smart-folder saved queries now use one canonical parser (`lib/search/saved_query.py`) that maps allowed JSON keys to `SearchFilters`; document list and `/api/v1/search` share `document_filter_sql` so smart folders cannot silently match all documents on unsupported keys.
- [2026-04-26] Phase 5 operational decision: uploads may continue enqueueing `ingest` jobs because `worker-ingest` is now a real consumer that acknowledges original assets and completes/fails jobs. Do not return to a placeholder ingest service while upload creates ingest jobs.
- [2026-04-26] Phase 5 contract decision: Phase 6 mutation placeholders stay visible in OpenAPI, but their POST contracts advertise `501` until Phase 6 implements contacts, filing rules, and watched folders. Runtime and contract parity is checked by unit tests.
- [2026-04-26] Phase 5 maintainability decision: the hardening pass extracted document-list filtering, document-summary mapping, and search filter UI state into cohesive modules to avoid regrowing god modules. Future Phase 6 work should follow the same pattern before adding contacts/rules/watched-folder logic.
- [2026-04-26] Phase 6 automation decision: keep contacts, filing rules, watched folders, shared ingestion, and operator maintenance in separate modules rather than appending to document/search/review routes. `apps/api/structura_api/routes_automation.py` and `routes_contacts.py` remain thin route layers.
- [2026-04-26] Phase 6 safety decision: filing-rule application reuses the manual organization rules through cursor-based action application, and rule evaluation blocks folder actions unless the acting user can write the target folder. High-stakes or review-required matches become suggestions instead of silent finalization.
- [2026-04-26] Phase 6 watched-folder decision: introduce a dedicated `worker-watched-folders` service and `automation` Compose profile. The worker scans stable PDF files under allowed paths and imports through `lib/documents/ingestion.py`; it does not own parsing, extraction, relationships, or search ranking.
- [2026-04-26] Phase 6 CLI decision: operator reprocess and search rebuild commands enqueue normal durable jobs via `lib/documents/maintenance.py`; they do not mutate parse/search tables directly and do not depend on placeholder admin HTTP calls.
- [2026-04-26] Phase 6 action-application hardening decision: allowed filing-rule actions are executed through `lib/automation/action_application.py`, which owns action dispatch for folder, tag, sensitivity, document-family, and review-task effects. Policy validation and runtime execution must stay in parity.
- [2026-04-26] Phase 6 transaction hardening decision: automation direct apply and suggestion accept use shared cursors so document organization, metadata changes, review-task creation, filing-rule run persistence, and suggestion decision updates are atomic. Projection refresh is post-commit.
- [2026-04-26] Phase 6 watched-root hardening decision: watched folders are constrained by `STRUCTURA_WATCHED_FOLDER_ROOT` and the worker rejects symlinked file candidates. Filesystem trust-boundary expansion requires an explicit future policy/ADR.
- [2026-04-26] Phase 6 contract hardening decision: deferred relationship, analysis, and export mutation endpoints stay visible but advertise `501` placeholder responses until their owning phases implement runtime behavior. Generated-client success contracts must not lead runtime implementation.
- [2026-04-26] Phase 6 CLI hardening decision: `bulk-import --execute` uses the authenticated document upload API with explicit token and URL validation, while maintenance commands use local durable job enqueues. CLI paths should enter normal product workflows instead of direct table mutation.
- [2026-04-27] Phase 7 relationship decision: implement relationships as first-class persistence/API/worker/UI rather than overloading filing-rule runs or search state. Relationship reads require ACL visibility for both linked documents; manual create/accept/reject requires writable access to both documents.
- [2026-04-27] Phase 7 deadline/timeline decision: derive deadlines from accepted canonical date fields, preserve selected-candidate confidence when available, expose deadlines in timeline/smart views/search facets, and keep timeline projection SQL separate from relationship mutation repositories.
- [2026-04-27] Phase 8 seam decision: visual retrieval, handwriting/Qwen routes, low-text fallbacks, and mixed text/visual ranking must be separate Phase 8 modules/workers. Do not fold visual work into `lib/relationships`, `workers/relationships`, Phase 4 extraction, or Phase 5 text embedding workers.
- [2026-04-27] Phase 8 implementation decision: difficult-document signals live in `lib/documents/quality.py` and are persisted under `metadata_json.phase8.quality` without replacing sibling metadata. Visual retrieval is exposed through the existing search contract via `mode: visual` and `includeVisual` for hybrid search, with ACL-filtered SQL in `lib/search/visual_repository.py`.
- [2026-04-27] Phase 8 embedding decision: text embeddings remain the default `embeddings` queue path. Selective visual embeddings use page-level owners, modality `visual`, deterministic profile `structura-fixture-visual-embedding:v1`, queue `visual-embeddings`, and the separate Compose `visual` profile/service `worker-visual-embeddings`.
- [2026-04-27] Phase 8 handwriting decision: handwriting/degraded/low-confidence pages route extraction through `qwen_primary_review_required`; the deterministic gateway marks those candidates as `sourceEngine=qwen3_vl_8b`, lowers confidence, and forces `needs_review` so canonical facts are not silently promoted.
- [2026-04-28] Phase 8.5 Blackwell model-runtime research decision: continue using `voipmonitor/vllm:cu130` for live Qwen/Granite/visual VLM serving on SM120 because the voipmonitor RTX 6000 Pro docs describe it as the recommended vLLM image with SM120 patches, FlashInfer, and Blackwell-specific fixes. Pair that with official vLLM guidance: reduce memory with `max_model_len` and `max_num_seqs`, use `gpu_memory_utilization` to trade KV-cache capacity versus co-residency, disable video for Qwen3-VL image-only workloads, set `OMP_NUM_THREADS=1` for multiple vLLM instances, and disable/size multimodal processor cache intentionally. Sources saved under `.firecrawl/`: `voipmonitor-docker-images.md`, `voipmonitor-vllm.md`, `vllm-optimization.md`, `vllm-conserving-memory.md`, and `vllm-qwen3-vl.md`.
- [2026-04-28] Phase 8.5 GPU placement decision: use Docker Compose GPU reservations with explicit `device_ids`/capabilities and `CUDA_DEVICE_ORDER=PCI_BUS_ID` rather than relying only on `gpus: all` plus `NVIDIA_VISIBLE_DEVICES`; Docker docs say unspecified `device_ids` exposes all GPUs, and NVIDIA docs say `NVIDIA_VISIBLE_DEVICES` controls enumeration. vLLM also requires device visibility to be correct before startup, so Structura model services should get deterministic GPU binding from Compose, not late in-process guessing.
- [2026-04-28] Phase 8.5 service-shape decision: do not assume all five live model surfaces should be permanently co-resident on two 24GB Blackwell cards. Keep Qwen3-VL-4B semantic annotation always-on, keep Granite 4.0 3B Vision high priority, allow visual embedding to co-reside with Granite only under measured KV/cache settings, and keep Qwen3-VL 8B disabled/deferred until a future explicit evaluation. Use the RTX 3090 node for text embeddings/lower-priority batch work when practical.
- [2026-04-28] Phase 8.5 runtime validation decision: live smoke/probe must validate the active services that are actually allowed to run. For the current Qwen3-VL-4B default, start Qwen3-VL-4B semantic, Granite, and visual embedding with ordered startup and measured co-residency; do not start Qwen3-VL 8B unless a future explicit HQ/rescue evaluation re-enables it.
- [2026-04-27] Phase 8-to-Phase 9 seam decision: Phase 9 analysis should consume `qualitySummary`, `sourceModalities`, visual/Qwen review flags, accepted canonical facts, and ACL-safe evidence refs. It must not treat unreviewed handwriting/visual output as authoritative analysis context or bypass `document_is_readable`.
- [2026-04-28] Phase 8.5 model-runtime decision: Qwen3-VL-4B and Granite 4.0 3B Vision are equal default-ingest priorities. Qwen3-VL-4B owns Docling-grounded semantic planning; Granite owns structured table/KVP/form extraction. Qwen3-VL 8B is disabled/deferred in the active runtime even though its explicit user-intent contracts remain visible for future evaluation. Text embeddings target Qwen3-Embedding at 1536 dimensions; live visual embeddings target native Qwen3-VL-Embedding 2048 dimensions unless a backend proves safe down-projection.
- [2026-04-28] Phase 8.5 corrected outcome decision: "failure" means pipeline/runtime failure, not difficult/garbled content. Degraded content should produce `insufficient_signal`, `needs_human_review`, or `no_extraction_target`; true failures need retry/dead-letter/admin health handling.
- [2026-04-28] Phase 8.5 architecture decision: `lib/model_runtime` owns model profiles/settings/HTTP/media/redaction/health/contracts; `lib/model_runtime/clients` owns model-service adapters; `lib/extraction/gateways` owns live Qwen/Granite routing; `lib/search/embeddings` owns live text/visual embedding adapters. Keep routes and repositories thin around these seams.
- [2026-04-29] Phase 8.5 extraction persistence decision: `document_extractions` now has document, semantic-region, and aggregate scopes. Current-row uniqueness is scoped by document/schema/scope for document and aggregate rows, and by document/schema/source semantic region for region rows.
- [2026-04-29] Phase 8.5 model-output decision: Granite task outputs use small model-output contracts under `contracts/model_outputs/`, while Structura owns canonical candidate mapping, validation, review policy, and provenance. Do not ask Granite to emit full app-owned canonical JSON directly.
- [2026-04-29] Phase 8.5 reconciliation decision: once all Granite region jobs for a semantic annotation are terminal, build a separate aggregate extraction from current region rows. Aggregates preserve extraction/semantic-region provenance, may use document-level Granite fields as fallback, and remain review-required even if JSON schema and arithmetic validation pass.
- [2026-04-29] Phase 8.5 Qwen3-VL-4B runtime decision: Smart Parse uses `qwen3-vl-4b-semantic:v1` with `max_model_len=24576`, `max_num_seqs=2`, `gpu_memory_utilization=0.84`, video disabled, and `limit_mm_per_prompt={"image":4,"video":0}`. Multi-image fan-in restores the historical 2B input shape; one-page windows are a coverage/context-length fallback, not the default.
- [2026-04-24] Phase 0 auth baseline uses Argon2id password credentials, durable `sessions`, non-HttpOnly CSRF cookie paired with HttpOnly session cookie, API-token principal resolution, and route dependencies that protect document, asset, job, and admin health surfaces.
- [2026-04-24] Phase 0 job baseline uses the `pipeline_jobs` table as the concrete queue state because the pinned ParadeDB PostgreSQL 17 image does not package PGMQ. Default workers expose internal health endpoints and record `service_health_snapshots`.
- [2026-04-24] Baseline migrations are tracked in `structura.schema_migrations`; legacy Phase 0 databases without that table are adopted by detecting representative schema objects, then future migration runs are no-ops.
- [2026-04-24] Phase 0 auth endpoints should let FastAPI/Pydantic handle session request validation so malformed bodies return 422, not unhandled 500s.
- [2026-04-24] Session and CSRF cookie names are configurable; all cookie reads must use `Settings.session_cookie_name` and `Settings.csrf_cookie_name`, not hardcoded aliases.
- [2026-04-24] Phase 0 keeps `pipeline_jobs` as the active queue ledger while recording an explicit fallback reason when the requested transport is PGMQ or Redis. Retryable failures use bounded exponential backoff before workers can claim them again.
- [2026-04-24] Phase 0 should expose protected skeletons for the active OpenAPI path set even when the owning feature phase is later. Generated FastAPI OpenAPI paths must match `contracts/api/openapi.yaml`; keep internal probes such as `/healthz` out of the public schema.
- [2026-04-24] Static analysis is part of the Phase 0 baseline: `make sast` runs Bandit, Semgrep, Pyright, and mypy, while Docker images run as non-root users.
- [2026-04-24] Structura's phase-specific execution plans are to be used as living implementation checklists. They deliberately include Fresh Context rereads and Firecrawl evidence instructions so future agents keep context current and support uncertain decisions with current primary-source evidence.
- [2026-04-24] Phase 11 and Phase 12 have different purposes: Phase 11 measures and produces release-candidate evidence; Phase 12 consumes that evidence to close blockers, freeze the release, hand off operations, and write the final go/no-go. Treat Phase 12 as the final planned phase.
- [2026-04-24] `STRUCTURA_PLAN_INDEX.md` and `STRUCTURA_IMPLEMENTATION_PLAN.md` now explicitly support Markdown-first artifact review with a parity note for the duplicate `01` and `02` DOCX pairs, so the root planning layer is consistent with the phase-specific plans and the user's clarified workflow.
